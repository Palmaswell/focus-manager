import * as React from 'react';
import { mount } from 'enzyme';
import { FocusContainer } from './focus-container';

describe('Focus Container', () => {
  test('initial tels', () => {
    const container = mount(<FocusContainer />);
  });
});
