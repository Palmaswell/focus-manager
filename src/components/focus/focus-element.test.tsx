import * as React from 'react';
import { mount } from 'enzyme';
import { FocusElement } from './focus-element';

describe('Focus Container', () => {
  test('initial tels', () => {
    const container = mount(<FocusElement />);
  });
});
